apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.kibana.name }}
  labels:
    {{- include "emk.labels" . | nindent 4 }}
    app.kubernetes.io/name: {{ .Values.kibana.name }}
spec:
  type: {{ .Values.kibana.service.type }}
  ports:
    {{- with .Values.kibana.service.ports }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  selector:
    {{- include "emk.labels" . | nindent 4 }}
    component: {{ .Values.kibana.name }}