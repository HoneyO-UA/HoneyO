apiVersion: v1
kind: Service
metadata:
  name: {{ include "emk.fullname" . }}
  labels:
    {{- include "emk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emk.selectorLabels" . | nindent 4 }}
