[Unit]
Description=HAProxy Spawner Worker
After=multi-user.target
Conflicts=getty@tty1.service

[Service]
User=root
Type=simple
Restart=always
ExecStart=/usr/bin/python3 {{ service_exec_path }}
EnvironmentFile={{ service.environment_file }}

[Install]
WantedBy=multi-user.target