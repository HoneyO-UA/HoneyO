apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "management-system.fullname" . }}-test-connection"
  labels:
    {{- include "management-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "management-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
