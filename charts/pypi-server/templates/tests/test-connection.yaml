apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pypi-server.fullname" . }}-test-connection"
  labels:
    {{- include "pypi-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pypi-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
