apiVersion: v1
kind: Service
metadata:
  name: {{ template "management-system.api" . }}
  labels:
    {{- include "management-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.k8sWorker.service.type }}
  ports:
    - port: {{ .Values.k8sWorker.service.port }}
      {{- if contains "NodePort" .Values.k8sWorker.service.type }}
      nodePort: {{ .Values.k8sWorker.service.nodePort }}
      {{- end }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "management-system.selectorLabels" . | nindent 4 }}
    component: {{ .Values.k8sWorker.name }}